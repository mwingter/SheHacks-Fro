<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Frô" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/mediumgreen</item>
        <item name="colorPrimaryVariant">@color/mediumgreen</item>
        <item name="colorOnPrimary">@color/lightgreen</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Se quiser mudar o espaçamento do menu vertical depois -->
        <!--<item name="android:listPreferredItemHeightSmall">20dp</item>-->

        <item name="android:listPreferredItemPaddingLeft">15dp</item>
        <item name="android:listPreferredItemPaddingRight">15dp</item>


        <item name="colorControlNormal">@color/black</item>
        <item name="colorControlActivated">@color/lightgreen</item>
        <item name="colorControlHighlight">@color/lightgreen</item>


    </style>

    <style name="DialogTheme" parent="@style/Base.Theme.AppCompat.Light.Dialog">
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="Theme.Frô.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/lightgreen</item>
    </style>

    <style name="Theme.Frô.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.Frô.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>